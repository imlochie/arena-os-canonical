# Arena server environment
# Copy to .env.local and replace the value with your PostgreSQL connection string.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/arena

# Optional provider keys. Local mode remains available without them.
OPENROUTER_API_KEY=
GROQ_API_KEY=
FORCE_LOCAL_MODE=0
