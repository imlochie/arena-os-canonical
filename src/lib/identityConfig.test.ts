import assert from "node:assert/strict";import test from "node:test";import {assertLocalBootstrapEnabled,identityConfig} from "./identityConfig";
test("development WebAuthn configuration is explicit",()=>{assert.deepEqual(identityConfig({NODE_ENV:"development"}),{rpId:"localhost",rpName:"Arena OS",origin:"http://localhost:3000",development:true});assert.doesNotThrow(()=>assertLocalBootstrapEnabled({NODE_ENV:"development",ARENA_LOCAL_IDENTITY_BOOTSTRAP:"1"}))});
test("production never silently downgrades identity",()=>{assert.throws(()=>identityConfig({NODE_ENV:"production"}));assert.throws(()=>identityConfig({NODE_ENV:"production",ARENA_WEBAUTHN_RP_ID:"arena.test",ARENA_WEBAUTHN_ORIGIN:"http://arena.test"}));assert.equal(identityConfig({NODE_ENV:"production",ARENA_WEBAUTHN_RP_ID:"arena.test",ARENA_WEBAUTHN_ORIGIN:"https://arena.test"}).development,false);assert.throws(()=>assertLocalBootstrapEnabled({NODE_ENV:"production",ARENA_LOCAL_IDENTITY_BOOTSTRAP:"1"}))});
