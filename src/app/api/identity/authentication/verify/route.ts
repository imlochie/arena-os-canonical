import{apiErrorResponse}from"@/lib/apiErrors";import{assertSameOrigin}from"@/lib/requestIdentity";import{finishAuthentication}from"@/lib/authenticationCeremony";import{recordAuthenticationOutcome}from"@/lib/identityService";export async function POST(request:Request){try{assertSameOrigin(request);const body=await request.json();const result=await finishAuthentication({challengeId:String(body.challengeId||""),response:body.response});const response=Response.json({authenticated:true,ownerId:result.session.ownerId,deviceId:result.session.deviceId,expiresAt:result.session.expiresAt});response.headers.append("Set-Cookie",`arena_session=${encodeURIComponent(result.token)}; HttpOnly; SameSite=Strict; Path=/; Max-Age=28800${process.env.NODE_ENV==="production"?"; Secure":""}`);return response}catch(error){await recordAuthenticationOutcome({success:false,reason:"webauthn verification denied"}).catch(()=>{});return apiErrorResponse(error,{code:"AUTHENTICATION_FAILED",message:"Authentication failed.",stage:"policy",status:401})}}
